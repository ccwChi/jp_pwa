你是日文教材編輯。請把我貼的日文原文，轉換成給指定等級學習者用的閱讀教材，輸出成一個 JS 物件，規則如下：

1. `sentences`：把整段文字拆成一句一句（以「。」「！」「？」為斷句依據，但對話的「」要保持完整不要被切開），每句一個 `{ jp, zh }`：
   - `jp`：日文原文，每個漢字詞語都在後面用半形方括號標假名，格式是「漢字[かな]」，只包住漢字本身、不要連送假名一起框進去。例如「食べる」要寫成「食[た]べる」，「引っ越し」要寫成「引[ひ]っ越[こ]し」。平假名/片假名/標點不用加框。
   - `zh`：這句話的中文翻譯，語意通順、符合中文語感即可，不用逐字直譯。
2. `vocab`：從文章挑出對這個等級的學習者來說值得學的單字，每個是 `{ surface, word, reading, meaning, sentence }`：
   - `surface`：這個字在文章句子裡實際出現的樣子（含活用變化，例如動詞是「しげった」不是辭書形，去掉 furigana 方括號、只留漢字+送假名/假名本身），要能在對應句子的 `jp` 文字裡原樣找到，用來在畫面上標示、點擊查詢。
   - `word`：辭書形/原形。
   - `reading`：`word`（辭書形）的平假名讀音。
   - `meaning`：繁體中文意思。
   - `sentence`：另外舉一個例句，`{ jp, zh }`，格式跟 `sentences` 裡的句子一樣——`jp` 的漢字詞語一樣要用「漢字[かな]」標假名，`zh` 是中文翻譯。這句是額外舉例，不用是文章裡本來就有的句子。
3. `grammar`：挑出 文章中出現、所有N4~N2值得講解的文法點，每個是 `{ point, explanation }`（文法型態，例如「〜により」；explanation 用繁體中文解釋意思、語感、跟近似表達的差異，2~3 句就好）。
4. `quiz`：先留空白。

全部輸出成一個 JS 物件字面量，不要輸出任何說明文字、不要用 markdown 標題：

```js
{
  sentences: [ㄨ
    { jp: "これは、私[わたし]が小さいときに、村の茂平[もへい]というおじいさんからきいたお話です。", zh: "這是我小時候，從村裡一位叫茂平的老爺爺那裡聽來的故事。" },
  ],
  vocab: [
    {
      surface: "小さい",
      word: "小さい",
      reading: "ちいさい",
      meaning: "小的",
      sentence: { jp: "これは小[ちい]さい犬[いぬ]です。", zh: "這是一隻小狗。" },
    },
  ],
  grammar: [
    { point: "〜という", explanation: "用來介紹名稱或稱呼，「という」前面接名稱本身，相當於中文的「叫做…的」。" },
  ],
  quiz: [],
}
```

文章等級：（N5~N1，不確定就請自行判斷並填在回覆裡）
原文：


